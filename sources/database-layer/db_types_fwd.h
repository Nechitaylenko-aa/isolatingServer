// db/db_types_fwd.h
//
// Обёртка над внешним <db_types.h> (SDBConnection/SSslConf/s_database_config,
// см. database.md). На момент написания Группы 3 физического заголовка
// библиотеки в проекте нет (см. tz_group3_database_layer.md §2.1/§6).
//
// Как только реальный <db_types.h> появится в include-путях проекта - этот
// файл начнёт подключать его напрямую, а локальный стаб (db_types_stub.h)
// просто перестанет собираться в цепочку включений. Менять поля
// SDBConnection/SSslConf здесь нельзя - они не наши, дословно из database.md.
//
// Решение подключать через __has_include, а не через макрос конфигурации
// сборки - осознанное отклонение от буквального `#include <db_types.h>` в
// db_thread.h из ТЗ (там это read-only псевдокод). Смысл тот же, но код
// компилируется и без реальной библиотеки, что нужно для юнит-тестов этой
// группы уже сейчас.

#pragma once

#include <db-types.h>
